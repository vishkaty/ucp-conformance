# Ambiguity / Discrepancy Register

Verified contradictions, version deltas, and under-specified areas. Each has a
**chosen interpretation + rationale**, and is **surfaced in the report as a flag** —
never silently resolved in the server's favor. Tests cite these ids (e.g. `AMB-001`).

| id | Issue | Evidence (pinned SHA) | Decision | Surfaced as |
|----|-------|-----------------------|----------|-------------|
| **AMB-001** | Version-incompatible status: spec says **422**, official suite asserts **400**. | `ucp:overview.md#L699` (422 mapping) vs `conformance:protocol_test.py::test_version_negotiation` (asserts 400). Suite is pinned to spec **2026-01-23**, predates 04-08 error table. | **Spec authoritative → accept 422.** If 400 observed, do not fail outright. | `advisory` flag: "server returned 400; spec 2026-04-08 maps version_unsupported→422; official suite (01-23) still expects 400". |
| **AMB-002** | The `v2026-04-08` git tag is dated **2026-05-22** and carries post-date cherry-picks. | `SOURCES.lock.json` (commit a2d8bf0b, date 2026-05-22). | Pin by SHA, treat the **tagged artifact** as "04-08"; never infer contents from the date. | Documented in SOURCES.lock + scope stamp shows the commit SHA. |
| **AMB-003** | Header scheme differs by version: 01-23 = `Request-Signature` (detached JWT, RFC 7797); 04-08 = RFC 9421 `Signature`/`Signature-Input`. `request-id` is suite-only (no spec basis). | `ucp:signatures.md` (04-08) vs 01-23 docs; `conformance:integration_test_utils.py::get_headers`. | **Version-gate** header expectations. Do NOT require `request-id`. Do NOT require `request-signature` on 04-08. | Per-version test wiring; report notes the scheme tested. |
| **AMB-004** | Totals invariants `sum(non-total)==total` and sub-line `sum(lines)==parent` are **prose-only, not in `totals.json`**. | `ucp:checkout.md` (prose) vs `ucp:schemas/shopping/types/totals.json` (no such constraint). | Implement as **coded checks** in addition to ucp-schema validation. | A passing ucp-schema validation does NOT imply totals consistency; both run. |
| **AMB-005** | `severity: unrecoverable` exists in 04-08 but **not** in 01-23's enum. | `ucp:schemas/.../message_error.json` (04-08 enum has 4 values) vs 01-23 (3). | **Version-gate** the accepted severity enum. | Report flags an `unrecoverable` severity when grading an 01-23 target. |
| **AMB-006** | `capabilities_incompatible` returns **HTTP 200 with error body**, not a 4xx. | `ucp:overview.md#L700` (200/result). | Expect 200-with-error-body; a 4xx here is a deviation. | `deviation` if a 4xx is returned for the no-intersection case. |
| **AMB-007** | A previously-claimed "platforms MUST strip scripts/untrusted HTML from product/variant descriptions" requirement does **NOT exist** in the pinned 04-08 spec. | `grep -rinE 'strip\|sanitiz\|untrusted\|script\|xss' docs/ source/schemas/` returns only schema-authoring boilerplate and the marketing homepage's own HTML — no normative rule. | **Do NOT add a conformance check for it** (testing a non-requirement = false-FAIL). The tester's OWN output escaping is a separate tool-security concern, not a UCP requirement. | n/a — requirement removed from scope; documented so it isn't reintroduced. |
| **AMB-009** | 01-era escalation-message severity: checkout.md prose (CHK-039 / ERR-008) requires "at least one message with **`severity: escalation`** when `status = requires_escalation`", but the pinned `message_error.json` severity **enum has no `escalation` value** (only `recoverable`, `requires_buyer_input`, `requires_buyer_review`). A schema-conformant 01-era server therefore CANNOT emit a `severity:escalation` message — the prose and the schema contradict. | `ucp-2026-01-23:docs/specification/checkout.md#L270-271` and `ucp-2026-01-11:...#L270` (prose "severity: escalation") vs `ucp-2026-01-23:source/schemas/shopping/types/message_error.json#L41-45` and `ucp-2026-01-11:...` (enum excludes `escalation`; the requires_* severities are the ones that "contribute to status: requires_escalation"). | **Do NOT convert CHK-039/ERR-008** to a check: modeling the literal requirement would force the golden to emit a schema-invalid severity (breaking the schema oracle), and grading the requires_* severity instead would assert something OTHER than the pinned MUST's words. The requirement is un-modelable by a schema-conformant server; it stays a documented GAP, not an all-skip check or a re-interpreted assertion. | GAP with reason (this row). Revisit if upstream reconciles the enum and the prose. |
| **AMB-008** | 04-08 webhook signed components: signatures.md's normative request table requires `ucp-agent` (when the header is present, SIG-018) and `idempotency-key` (POST, SIG-016) as signed components, but order.md's webhook example signs only `("@method" "@authority" "@path" "content-digest" "content-type")` while carrying a UCP-Agent header — and webhooks carry `Webhook-Id`, not `Idempotency-Key`. | `ucp:signatures.md#L190-206` (component table) vs `ucp:order.md#L478-486` (webhook example). | The controlled fixture signs the FULL normative set (ucp-agent + idempotency-key=Webhook-Id + @query); the webhook checks assert only the UNAMBIGUOUS core (`@method @authority @path` SIG-014, `content-digest content-type` SIG-015, `@query` when the URL has one SIG-017). **No check asserts SIG-016/SIG-018 on webhooks** — a merchant following the order.md example would false-DEVIATE. | SIG-016/SIG-018 stay uncovered for webhooks; documented so a stricter check isn't reintroduced without an upstream spec fix. |
| **AMB-010** | `UCP-Agent` without `version=`: which version does the business serve? 08-25 negotiation text describes the platform's version as coming from its `UCP-Agent` `version=` parameter; when the parameter is absent the text does not say whether to read the platform profile's `version`, assume the business's own, or reject. | `ucp-2026-08-25:docs/specification/overview/index.md` (NEG table, OVR-071 / NEG-001: rejection is defined only for a version the business does not advertise). | **Read the platform profile's `version`; absent there too, assume the business's own version** (owner decision 21, 2026-09-10). golden-0825 implements the second half today (`dependencies.validate_ucp_headers`); the profile read lands with D3-24. | golden-0825 assumes its own version on an absent parameter; no check asserts either reading until D3-24. |
| **AMB-011** | Is a request for a route the business does not serve at all (no UCP operation behind it) "an operation" whose error MUST be a UCP error envelope? The 08-25 error text defines the envelope for operation errors; it is silent on non-route 404s. | `ucp-2026-08-25:docs/specification/overview/index.md` (error envelope, #L1835-L1911) and `source/schemas/common/types/error_response.json`. | **Serve the envelope on every error, non-route 404s included** (golden-0825, D3-04): a superset is harmless to a conformant platform and removes the framework-shaped body a platform cannot parse as UCP. No check REQUIRES the envelope on a non-route 404 (golden_check_08_25.py's ERR-028.unknown-route grades our own golden only, unattributed). | golden-0825 answers `/nope` with a 404 envelope; no public check asserts either reading. |
| **AMB-012** | Are `spec`/`schema` REQUIRED on every capability entry a **business** advertises at 2026-04-08? overview.md prose says REQUIRED "for all capabilities"; the pinned `capability.json` requires them only on `platform_schema` (`business_schema` and `response_schema` do not). | `ucp:docs/specification/overview.md#L82` ("The `spec` and `schema` fields are **REQUIRED** for all capabilities.") vs `ucp:source/schemas/capability.json#/$defs/platform_schema/allOf[1]` (`required: [spec, schema]`; absent from `$defs/business_schema`) at pin a2d8bf0b. | **The register follows the prose** (OVR-002, REQUIRED, graded by `profile.spec_url_authority` since 2026-09-10 under the owner ruling adopting W0-review ruling (a)): a business capability entry without `spec`+`schema` is a deviation. Both official reference servers (flower golden pin 00333a80, Node reference) and the golden-0825 04-08 profile carry both on every entry; our controlled fixture was corrected to do the same (W0 B4). | Not a flag: `profile.spec_url_authority` deviates outright on a capability entry missing either field (OVR-002 is graded, not annotated); this row records why the register reads the prose over the schema. |
| **AMB-013** | Over MCP, which REST outcomes are JSON-RPC *errors* and which are `result` envelopes? The 08-25 transport table maps 401/403/409(idempotency)/429/500/503 and the negotiation/signature codes to JSON-RPC codes and says business outcomes are `result`s, but is silent on a request the UCP schemas reject (REST 422 `invalid_request`) and on 404 `not_found`. | `ucp-2026-08-25:docs/specification/overview/index.md#L1857-L1886` (transport binding tables), `shopping/checkout/mcp.md#L802-L812` (conformance items 3-5: "Validate tool inputs against UCP schemas"), `shopping/catalog/mcp.md#L715` (-32602 for over-limit batch requests). | **golden-0825 (D3-09)**: a schema-rejected input (REST 422 `invalid_request`) is `-32602` invalid params, like the catalog binding's over-limit rule; `not_found`, `out_of_stock`, `payment_failed`, `checkout_not_modifiable` and every other 4xx business envelope are `result`s. No check asserts the 422 reading; `mcp.business_outcome_as_result` asserts only `out_of_stock` (explicitly a business outcome in checkout/mcp.md). | golden-0825 answers -32602 for a schema-invalid tool input; a merchant that returns the envelope as a `result` there is not flagged. |

## How flags render
A flag is a non-failing annotation attached to a check result. It appears in the
report next to the affected check, carries the `AMB-id` and a one-line explanation,
and is counted in the summary. Flags never flip an aggregate verdict by themselves,
but a check that depends on an ambiguous interpretation can only be `clean-pass`
when it matches the **spec-authoritative** reading; any other reading is `advisory`
or `deviation` per the table above.

## DSC-003 — case-insensitive discount codes (reference server does NOT satisfy)
The spec (2026-01-23 DSC-003) says discount codes MUST be matched case-insensitively.
The Flower Shop reference server rejects a lowercased form of a valid code (e.g. `10off`
for seeded `10OFF`), so it does not implement DSC-003. A check for this deviates on the
known-good server and therefore cannot be reference-gated against it; deferred until a
golden that implements case-insensitive matching is available. Discovered 2026-07-01.

## ERR-001 — error message envelope (reference server uses a different shape)
Spec 2026-01-23 ERR-001 says an error message MUST include type, code, content, and
severity. The Flower Shop reference server instead returns `{"detail": "...", "code":
"..."}` on a 400 (e.g. out-of-stock). So it satisfies VAL-006 (populated `detail`) but
NOT the ERR-001 four-field envelope. A strict ERR-001 check would deviate on the
known-good server, so it's not reference-gateable against Flower Shop; deferred.
Discovered 2026-07-01.
