{
 "_about": "Agent-side adversarial-review sign-offs: every agent CHECK id must be reviewed before it counts \u2014 mirrors the merchant review-signoff gate.",
 "signoffs": [
  {
   "batch": "agent-P0-B1-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "author review vs pinned discovery spec (presence check; independent-agent review reserved for the substantive signature/OAuth checks in B.2/B.3)",
   "spec_reverified": true,
   "notes": "agent.sends_ucp_agent cites DISC-006 ('Platforms MUST include their profile URI in every request'). Check asserts every API request (create_checkout) carries a UCP-Agent header declaring a profile; deliberately excludes the unauthenticated discovery GET (where the profile is first learned) to avoid over-strictness. Subject = platform/agent (correct). Kill-safe: the no_ucp_agent mutant omits the header -> DEVIATION.",
   "ids": {
    "2026-04-08": [
     "DISC-006"
    ]
   }
  },
  {
   "batch": "agent-P0-B2-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "author review vs pinned checkout spec (behavioral flow check; independent-agent review reserved for the crypto/OAuth checks in B.3)",
   "spec_reverified": true,
   "notes": "agent.follows_escalation cites CHK-008 ('Platform MUST use continue_url when checkout status is requires_escalation'). The adversarial sandbox returns requires_escalation + a continue_url it serves; the check requires the agent to have made a request to exactly that continue_url. Subject = platform/agent (correct). Kill-safe: the ignore_escalation mutant skips the follow -> DEVIATION. Hermetic (in-process sandbox controls the continue_url).",
   "ids": {
    "2026-04-08": [
     "CHK-008"
    ]
   }
  },
  {
   "batch": "agent-P0-B3-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "INDEPENDENT adversarial review (agent) re-read signatures.md + ap2-mandates.md + the crypto",
   "spec_reverified": true,
   "notes": "agent.verifies_business_signature: sandbox signs RFC 9421 responses (bad_signature scenario corrupts the Signature); the reference agent verifies via common/crypto.verify_response and rejects. Independent review REFUTED the original PAY-036 citation (that is the AP2 merchant_authorization JWS/JCS mandate signature, a DIFFERENT mechanism \u2014 JCS-canonicalized, body-embedded, AP2-gated) and confirmed the correct rows: SIG-036 (signatures.md L404: 'Verification must reconstruct the signature base and reject with signature_invalid if ECDSA verification fails'; L444 mirrors for responses) + SIG-002 (L90: 'All implementations MUST support verifying ES256'). Fixed before locking. Crypto anchored by crypto-interop (common/crypto == fixture's openssl-cross-anchored ES256). Kill-safe: skip_sig_verify -> agent accepts the invalid signature -> DEVIATION.",
   "ids": {
    "2026-04-08": [
     "SIG-036",
     "SIG-002"
    ]
   }
  },
  {
   "batch": "agent-P0-B4-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "INDEPENDENT adversarial review (agent) re-read identity-linking.md + the OAuth flow; author citation-check corroborated",
   "spec_reverified": true,
   "notes": "Two OAuth checks under the '### For Platforms' section (identity-linking.md L96, governing L98-155) — correct Platform subject for the agent lane; IDL-011/IDL-012 were reclassified 'manual' in the merchant register precisely because they bind the platform (request author), not the business. (1) agent.uses_pkce cites IDL-011 (L135-137: 'MUST use PKCE ... with code_challenge_method=S256 for all authorization code exchanges'). Asserts the authorization request carries code_challenge + code_challenge_method=S256; requiring S256 explicitly is mandated (plain forbidden, IDL-049 L570) and RFC 7636 defaults an omitted method to plain. Kill-safe: no_pkce omits both params -> DEVIATION. (2) agent.validates_iss cites IDL-012 (L138-144: 'MUST validate the iss parameter ... verify that the iss value matches the authorization server's issuer URI ... If the values do not match, the platform MUST abort and discard'). Runs against the bad_iss sandbox (iss=https://mixup-attacker.example != issuer); the reference compares iss == discovered metadata issuer (RFC 9207 AS issuer identifier, NOT the token endpoint — confirmed correct) and rejects. Kill-safe: skip_iss_validation -> agent accepts mismatched issuer -> DEVIATION. Non-blocking notes: sandbox returns {code,state,iss} as JSON (transport-only simplification; iss-compare logic identical to a real redirect); scope is authorization-request/response observables (the business-side PKCE-enforcement / iss-emission counterparts live in the merchant lane).",
   "ids": {
    "2026-04-08": [
     "IDL-011",
     "IDL-012"
    ]
   }
  },
  {
   "batch": "agent-P0-B5-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "INDEPENDENT adversarial review (agent) re-read signatures.md + the request-signer; found 2 real problems, both FIXED before locking; author re-verified citations verbatim + defect isolation",
   "spec_reverified": true,
   "notes": "The reference agent now SIGNS its own RFC 9421 requests (before it only verified business responses); the sandbox verifies them (agent_jwks) and 401s an invalid one, so signing is round-trip anchored. Independent review of the request-signing family REFUTED the initial design on two points and both were fixed before this sign-off: (1) MISCITATION — a single check citing SIG-001 asserted the covered-component SET; SIG-001 (L42) is only the architectural umbrella ('UCP uses HTTP Message Signatures for all transports') and names no components. Fixed by re-citing to the precise register rows and splitting into per-row checks: SIG-014 (L193: 'REST request signed components MUST include @method, @authority, @path'), SIG-015 (L199: 'MUST include content-digest and content-type when the request has a body'), SIG-016 (L198: 'idempotency-key MUST be a signed component for POST/PUT/DELETE/PATCH'), SIG-018 (L197: 'ucp-agent MUST be a signed component if the UCP-Agent header is present'). SIG-001 is retained as a distinct check that the signature REALLY VERIFIES (crypto-anchored by the sandbox receiver), mirroring the merchant lane's own SIG-001 'signature.rfc9421_response_verifies'. (2) OVER-STRICTNESS — the original treated an UNSIGNED request as a MUST defect, but request signing is SHOULD (SIG-026, L495: 'Platforms SHOULD sign all requests ... Alternative authentication mechanisms (API keys, OAuth, mTLS) may be used instead'). Fixed: unsigned requests are now skipped (never failed); the checks are conditional-on-signing (WHEN the agent signs, the signature MUST verify and MUST cover the required components), and the kill mutations are a signing agent that corrupts the signature or drops a required component (sign_corrupt / sign_omit_authority / sign_omit_digest / sign_omit_idem / ucp_agent_not_signed) — never one that declines to sign. Defect isolation verified diagonal (each mutation trips exactly its own row's check). Component scheme matches the merchant webhook signer (p_webhook_components, SIG-014/015). Denominator grew honestly to 101 (SIG-014/015/016 added to AGENT_EXTRA as platform-request-signer obligations; SIG-011 proxy-re-serialization and SIG-022 idempotency-entropy remain honest gaps, not reverse-harness-testable).",
   "ids": {
    "2026-04-08": [
     "SIG-001",
     "SIG-014",
     "SIG-015",
     "SIG-016",
     "SIG-018"
    ]
   }
  },
  {
   "batch": "agent-P0-B6-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "INDEPENDENT adversarial review (agent) re-read identity-linking.md + RFC 6750; checks SOUND, one sandbox-fidelity PROBLEM found and FIXED before locking; author verified citations verbatim + diagonal isolation",
   "spec_reverified": true,
   "notes": "The RFC 6750 WWW-Authenticate: Bearer 401-challenge flow (a gated /orders op in the auth_challenge sandbox). All three rows are under '### For Platforms' (identity-linking.md L96) — correct agent subject; they are 'manual' in the merchant register because they bind the platform (request author), so the agent lane is their home. Citations verbatim: IDL-007 (L121-123: 'MUST include user identity tokens in the HTTP Authorization header using the Bearer scheme: Authorization: Bearer <access_token>') -> agent.sends_bearer_token; IDL-008 (L124-126: 'MUST process WWW-Authenticate: Bearer challenges per RFC 6750 section 3 on 401 and 403 responses to user-authenticated operations') -> agent.processes_auth_challenge; IDL-009 (L127-128: 'MUST extract the scope parameter (when present) ... to construct subsequent authorization requests') -> agent.extracts_challenge_scope. Independent review confirmed all three SOUND with correct req_ids (no better match; IDL-051 only partially overlaps IDL-009) and DIAGONAL kill-isolation (no_bearer_retry->IDL-008 only via the vacuous-CLEAN-when-prerequisite-absent design; no_bearer_header->IDL-007; ignore_challenge_scope->IDL-009). The review found one genuine PROBLEM in the sandbox STIMULUS (not the checks), FIXED before this sign-off: the 401 challenge used error=\"identity_required\" (not an RFC 6750 section 3.1 error code — only invalid_request/invalid_token/insufficient_scope are valid; and a no-token 401 should omit error per spec L429-430) and a non-schema scope token orders.read. Rebuilt as the faithful two-round flow: 401 identity_required (no error, no scope; spec L448-450) -> initial token -> 403 insufficient_scope carrying scope=dev.ucp.shopping.order:read (valid IDL-037 token; spec L516-519) -> incremental re-authorization -> 200. This also closed the review's noted coverage gap (IDL-008 binds 401 AND 403; the 403 half is now exercised) and the nit that IDL-009 compared a hard-coded constant (it now derives the expected scope from the logged challenge). Business-side scope enforcement remains the merchant lane's job.",
   "ids": {
    "2026-04-08": [
     "IDL-007",
     "IDL-008",
     "IDL-009"
    ]
   }
  },
  {
   "batch": "agent-P0-B7-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "INDEPENDENT adversarial review (agent) re-read identity-linking.md + RFC 7636/8252; all three SOUND, one non-blocking hardening applied; author verified citations verbatim + off-diagonal isolation empirically",
   "spec_reverified": true,
   "notes": "OAuth public-client + authorization-response hygiene, all under '### For Platforms'. IDL-004 (L111-116: public clients — incl. on-device agent runtimes per RFC 8252 section 8.5 — 'MUST use none and rely on PKCE with S256 as proof-of-possession of the authorization code') -> agent.public_client_pkce_proof asserts the token exchange presents a code_verifier (the RFC 7636 proof; IDL-011 already grades the code_challenge/S256 half at /authorize). IDL-005 (L116-117, MUST NOT: 'Public clients MUST NOT embed a client_secret') -> agent.no_client_secret. IDL-035 (L301-305: 'the platform MUST verify that the state parameter matches the value sent ... and that the iss parameter matches ... If either check fails, the platform MUST discard the authorization response') -> agent.validates_oauth_state grades the STATE half via a new bad_state sandbox scenario (tampered state echo, iss left correct so it rejects on state alone); the iss half stays with IDL-012 (bad_iss) — clean decomposition of a compound row across two scenarios (IDL-035 is the only MUST row requiring state verification; IDL-013 is merely SHOULD-include-state). This also completed the reference agent's PKCE flow (it now presents the verifier at /token, not just the challenge at /authorize). Independent review confirmed all three SOUND with correct req_ids and empirically-verified diagonal isolation (no_pkce_verifier->IDL-004, embed_client_secret->IDL-005, skip_state_validation->IDL-035, each trips ONLY its own check). One non-blocking hardening APPLIED per the review: p_no_client_secret now also rejects a client_secret hidden in an Authorization: Basic header (client_secret_basic — the spec's own token example uses that form) or the query string, not just the body — robustness for grading arbitrary agents. Known modeling caveat (shared with IDL-012): 'rejected' models detection; run_flow doesn't literally abort after authorize — adequate as a detection signal.",
   "ids": {
    "2026-04-08": [
     "IDL-004",
     "IDL-005",
     "IDL-035"
    ]
   }
  },
  {
   "batch": "agent-P0-B8-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "INDEPENDENT adversarial review (agent) re-read identity-linking.md L236-257 + RFC 8414; both SOUND, one modeling note addressed; author verified citations verbatim + isolation empirically",
   "spec_reverified": true,
   "notes": "RFC 8414 authorization-server metadata discovery (a new discover_oauth_metadata() step in run_flow fetches /.well-known/oauth-authorization-server and populates identity from it). IDL-033 (L253-257: 'The issuer value in the discovered metadata MUST match the discovery base URI exactly ... Platforms MUST NOT normalize (e.g., strip trailing slashes) before comparison — the value must be a byte-for-byte match') -> agent.validates_issuer_match: against the bad_issuer sandbox (issuer = base + trailing slash) the agent compares issuer == self.server (the discovery base the well-known was fetched from) byte-for-byte and rejects; kill normalize_issuer does an rstrip('/') compare (the forbidden normalization) and wrongly matches. IDL-031 (L240-241: 'Any other non-2xx response, network error, or timeout: MUST abort. MUST NOT proceed to step 2') + IDL-032 (L249-251: 'Platforms MUST NOT silently fall through on any error other than 404 in step 1') -> agent.aborts_on_discovery_error: against the discovery_error sandbox (a 500) the agent aborts and does NOT fetch the OIDC fallback; kill oidc_fallthrough_on_error wrongly proceeds to OIDC. A legitimate 404 still falls through (not penalized). Independent review confirmed both SOUND, correct Platform subject (register marks these platform-bound), correct req_ids (no closer match; IDL-031/032 are the restated-pair for abort/no-fallthrough), fair stimuli, genuine kill-safe defects, isolated (separate scenarios). Review's one non-blocking modeling note ADDRESSED: run_flow now truly HALTS the flow on a discovery abort/issuer-rejection (was continuing with stale profile identity) — models IDL-031's 'MUST abort the identity-linking process' fully. No interference with IDL-011/012 (RFC 8414 metadata carries the same issuer/endpoints; bad_iss still tampers only the /authorize response so validates_iss still fires).",
   "ids": {
    "2026-04-08": [
     "IDL-031",
     "IDL-032",
     "IDL-033"
    ]
   }
  },
  {
   "batch": "agent-P0-B9-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "INDEPENDENT adversarial review (agent) re-read checkout.md/order.md/overview.md; new check + 2 citation extensions all SOUND, every extended citation genuinely EXERCISED (no coverage inflation); author verified quotes verbatim",
   "spec_reverified": true,
   "notes": "Checkout/order lifecycle increment. NEW CHECK agent.no_autocomplete_mismatched_totals cites CHK-055 + TOT-010 (both quote the identical sentence at checkout.md#L811: 'platforms MUST NOT autonomously complete a checkout with mismatched totals' — two distinct denominator rows, checkout-lifecycle vs totals area, indexing the same normative sentence; both Platform-subject). _totals_consistent() is a byte-faithful transcription of the spec's own assert (checkout.md L806: sum(e.amount for non-'total') == total.amount); the mismatched_totals sandbox returns subtotal=1000/tax=100/total=9999 (1100!=9999, two well-known additive types). Predicate requires DETECTED-mismatch AND NOT-completed (the positive-detection conjunct closes a trivial-pass hole a bare not-completed would leave). Kill complete_on_mismatch bypasses the guard and completes -> DEVIATION. CITATION EXTENSIONS (existing predicates unchanged, only req_ids grew, each verified genuinely exercised): agent.sends_ucp_agent += CHK-046 (checkout-rest.md#L1237 'All requests MUST include the UCP-Agent header') + ORD-015 (order.md#L395 'MUST include UCP-Agent header with profile URL on all requests') — create_checkout/complete are checkout requests and GET /orders (fetch_gated, issued in the conformant scenario where this check runs) is an order request, all carrying UCP-Agent and in the graded api list. agent.follows_escalation += PAY-017 (overview.md#L1409 'The platform MUST now open continue_url in a WebView/Window ... then retry the completion') — the escalate_token->requires_escalation+continue_url stimulus is a faithful 3DS/SCA soft-decline, and following the exact continue_url is the load-bearing MUST. NON-BLOCKING CAVEATS recorded by the review (no code change): (1) verification is a MAY (checkout.md L803) so requiring the detection flag is marginally stronger than the bare MUST NOT — accepted tradeoff vs the trivial-pass hole; (2) PAY-017's secondary 'then retry the completion' sub-clause is unexercised (only the primary open-continue_url MUST is graded). All five rows Platform-subject (the merchant lane exempts them as client-bound).",
   "ids": {
    "2026-04-08": [
     "CHK-046",
     "CHK-055",
     "ORD-015",
     "PAY-017",
     "TOT-010"
    ]
   }
  },
  {
   "batch": "agent-P0-B10-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "INDEPENDENT adversarial review (agent) re-read payment-handler-guide.md/overview.md + payment schemas; SOUND after 2 fixes applied before locking; author verified structure vs the pinned schema",
   "spec_reverified": true,
   "notes": "agent.honors_available_instruments cites PAY-009 (payment-handler-guide.md#L303-304: 'Platforms MUST treat it as authoritative and MUST NOT attempt to use instrument types ... that contradict it') + PAY-010 (overview.md#L1203: 'Platforms MUST treat the available_instruments in the response as authoritative for that checkout'). The check asserts the complete request's credential instrument `type` is one the checkout advertised, and that a type is present at all (payment_instrument.json requires type). Kill use_unavailable_instrument pays with crypto_wallet (not offered) -> DEVIATION. PAY-008 DELIBERATELY EXCLUDED (it bundles the whole response_schema/handler config — tokenization specs, merchant identity — which this check does not exercise; PAY-009/010 pin the available_instruments-authority precisely) — review confirmed this conservative call correct. TWO REVIEW FINDINGS FIXED BEFORE LOCKING: (1) STRUCTURAL — the first cut placed available_instruments at a fabricated flat `payment.available_instruments`; the pinned schema puts it ONLY under `ucp.payment_handlers.<handler_name>[].available_instruments` (payment_handler.json#L18; overview.md examples), which IS the response_schema PAY-009 names. Fixed the sandbox + agent reader + predicate to read the union of types across `ucp.payment_handlers.*[].available_instruments` (would otherwise false-fail a real merchant). (2) KEYWORD — retagged the ACheck keyword MUST -> MUST NOT to match PAY-009 and the kill semantics (paying with an unoffered type is a prohibition violation). Both Platform-subject. Kill-safe with all evasions fail-closed (no create, no complete, absent available_instruments, untyped instrument all -> DEVIATION).",
   "ids": {
    "2026-04-08": [
     "PAY-009",
     "PAY-010"
    ]
   }
  },
  {
   "batch": "agent-waveA-c1-2026-07-03",
   "date": "2026-07-03",
   "reviewer": "Wave A parallel design (independent design agent + batch completeness-critic) THEN an INDEPENDENT implementation review of the built checks; author verified citations verbatim + diagonal isolation empirically",
   "spec_reverified": true,
   "notes": "First serial-integration cluster of the parallel-coverage plan (design cards in ops/agent-waves/wave-a-cards.json). Both under identity-linking.md '### For Platforms' (agent subject). IDL-010 (L132-134: 'MUST implement the OAuth 2.0 Authorization Code flow (RFC 6749 section 4.1) as the account linking mechanism') -> agent.uses_authorization_code_flow asserts every authorize/authorize_gated op carries response_type=code and every token op grant_type=authorization_code; kill implicit_grant sets response_type=token (RFC 6749 section 4.2 implicit — a different flow). IDL-002 (L98-100: 'MUST authenticate token endpoint requests using a method advertised in the business's token_endpoint_auth_methods_supported metadata') -> agent.uses_advertised_auth_method infers the token-request method (Basic->client_secret_basic, body.client_secret->client_secret_post, client_assertion->private_key_jwt, else none) and requires it in the advertised set; the sandbox RFC 8414 metadata advertises [none, client_secret_basic, client_secret_post] and the public-client reference uses 'none'; kill unadvertised_auth_method authenticates with private_key_jwt (unadvertised) while keeping code_verifier and no client_secret. Independent implementation review returned BOTH SOUND, no fixes required — faithful citations, correct subject, genuine violations, fail-closed guards (absent metadata / no token op -> DEVIATION), and empirically-verified one-check isolation (implicit_grant trips ONLY IDL-010; unadvertised_auth_method ONLY IDL-002). Two cosmetic notes recorded (IDL-010's grant_type half is vacuous in the conformant scenario since /orders is ungated there — belt-and-suspenders; _token_auth_method doesn't model tls_client_auth/client_secret_jwt — out of sandbox scope, code-commented). Denominator membership unchanged (both were already gaps) so the drift gate stays green.",
   "ids": {
    "2026-04-08": [
     "IDL-002",
     "IDL-010"
    ]
   }
  },
  {
   "batch": "agent-waveA-c2-2026-07-04",
   "date": "2026-07-04",
   "reviewer": "Wave A parallel design card + INDEPENDENT implementation review; author verified citations verbatim + config.scopes shape vs the spec example + diagonal isolation empirically",
   "spec_reverified": true,
   "notes": "Config/scope cluster (Wave A). Both under identity-linking.md '### For Platforms'. IDL-034 (L297-299: 'Platforms MUST request only the derived scope set — not a superset') -> agent.requests_only_derived_scopes: the linking authorize's UCP scope tokens (those with ':') MUST be a subset of the advertised config.scopes ceiling; grades the SUPERSET direction only (a subset/empty request is conformant per Scope Derivation step 3 L387-394); kill request_scope_superset appends dev.ucp.shopping.order:admin (not in config.scopes). The sandbox profile now advertises capabilities['dev.ucp.common.identity_linking'][0].config.scopes = {order:read:{}, order:manage:{}} — a wire-scope->policy-object map matching the spec example L714-722 byte-for-byte — and the reference agent's main authorize requests exactly that derived set (was 'openid'). IDL-057 (L651-654: 'When config contains fields not defined in this version of the spec (providers or any other future field), platforms MUST ignore those fields and proceed using OAuth 2.0 with RFC 8414 discovery') -> agent.ignores_future_config (new future_config scenario injects an unrecognized `providers` field): the agent recorded a non-empty unknown_config_fields AND still performed as_discovery (the RFC 8414 op) + authorize (did not abort); the predicate is NOT coupled to the field name (any key other than scopes trips it); kill abort_on_future_config returns after discover. Independent review returned BOTH SOUND, no fixes — faithful citations, correct platform subject, genuine kills, spec-accurate shapes, no trivial pass, and confirmed the main-authorize scope change ('openid'->derived) perturbs NO existing check (only IDL-034 reads scope; IDL-009 reads authorize_gated; IDL-011/012/035 read PKCE/iss/state) and that adding the identity_linking capability perturbs nothing (no existing predicate reads capabilities). Denominator membership unchanged (both were gaps) so the drift gate stays green.",
   "ids": {
    "2026-04-08": [
     "IDL-034",
     "IDL-057"
    ]
   }
  },
  {
   "batch": "agent-waveA-c3a-2026-07-04",
   "date": "2026-07-04",
   "reviewer": "Wave A design card + INDEPENDENT implementation review (empirically proved IDL-061 distinctness); author verified citations verbatim + diagonal isolation",
   "spec_reverified": true,
   "notes": "Discovery/iss cluster. IDL-062 (identity-linking.md L246-247: 'Any non-2xx response, network error, or timeout: MUST abort the identity linking process' — the OIDC Discovery FALLBACK step 2) -> agent.aborts_on_oidc_fallback_error (new oidc_fallback_error scenario: RFC 8414 404s -> OIDC fallback 500s): the agent aborts and does NOT proceed to authorize/token; kill oidc_fallback_no_abort (discover_oidc skips the abort). Predicate grades only identity-linking ops (authorize/token), not the checkout, matching the spec's 'abort the identity linking process'. Genuinely distinct from IDL-031/032 (discovery_error = RFC 8414 non-404, no fall-through). IDL-061 (L605-607: 'The issuer value in RFC 8414 metadata and the iss parameter in authorization responses MUST be identical (byte-for-byte). Platforms MUST NOT normalize before comparison') -> agent.no_iss_normalization (new iss_normalized scenario: authz iss = issuer + '/', metadata issuer byte-equal to base so IDL-033 not tripped); kill normalize_iss compares rstrip('/') both sides. The review EMPIRICALLY PROVED IDL-061 is NOT a double-count of IDL-012/IDL-033: normalize_iss evades IDL-012 (in bad_iss the mismatch is gross, still rejected) while failing IDL-061 — it catches a bypass IDL-012 cannot see; distinct value pair from IDL-033 (authz-iss-vs-metadata-issuer, not issuer-vs-base). Both SOUND, no fixes. Regression-checked: the discover_oidc change (adopt-on-2xx / abort-on-non-2xx) + generalized run_flow halt (as_discovery OR oidc_discovery) did NOT break IDL-031/032. Cosmetic-only nits: the register row `source` start lines could be tighter (IDL-062 #L246-247, IDL-061 #L605-607) — quotes + req_ids are correct. Denominator membership unchanged (drift gate green).",
   "ids": {
    "2026-04-08": [
     "IDL-061",
     "IDL-062"
    ]
   }
  },
  {
   "batch": "agent-waveA-c3b-2026-07-04",
   "date": "2026-07-04",
   "reviewer": "Wave A design cards + INDEPENDENT implementation review; author verified citations verbatim + diagonal isolation + applied both cosmetic fixes the review suggested",
   "spec_reverified": true,
   "notes": "Revocation + pre-baked-authz cluster. agent.revokes_token_on_unlink co-cites IDL-014 (L149-151: 'MUST call the business's token revocation endpoint (RFC 7009) ... when a user initiates an unlink action on the platform side') + IDL-055 (L617-618: 'Platforms MUST revoke user identity tokens at the business's revocation endpoint (RFC 7009) when a user unlinks their account') — the register's own IDL-014 note says 'Restated at Security Considerations L617-L618', so they are the SAME platform obligation (safe to co-cite; IDL-055's business half 'Businesses MUST reject subsequent requests' is graded merchant-side). The sandbox advertises a revocation_endpoint + serves POST /oauth2/revoke; the reference agent's unlink() (called after fetch_gated) POSTs each session-issued token; kill skip_revocation returns without POSTing. The predicate ties the revoked token back to a session-issued access_token (can't pass by revoking a fabricated token). RFC 7009 form-encoding vs signed-JSON: the review confirmed encoding is not what IDL-014/055 emphasize and signed-JSON is the coherent UCP transport — no fix. agent.owns_authz_request cites IDL-044 (L440-443: 'continue_url MUST NOT be used to convey a pre-baked OAuth authorization request; the platform constructs its own authorization request ... including PKCE values, state, and redirect_uri it owns') — DUAL; the MUST NOT binds the business emitter (merchant-graded), the agent half is 'constructs its own', so the check grades that the authorize_gated redirect_uri/state/code_challenge ALL differ from the pre-baked (attacker) continue_url triple; kill adopt_prebaked_authz copies them (account-takeover). Independent review: BOTH SOUND, kill-safe, cleanly isolated, non-vacuous. Applied its two optional fixes: retagged IDL-044 keyword MUST NOT -> MUST (the agent-side obligation is affirmative 'constructs its own'); hardened the revoke predicate's token-body .get against a null body. Regression-checked: unlink()/revoke ops don't perturb p_sends_ucp_agent (revoke is UCP-Agent + RFC-9421 signed); the prebaked continue_url doesn't perturb IDL-007/008/009 (auth_challenge has no continue_url on its 401). Denominator membership unchanged (drift gate green).",
   "ids": {
    "2026-04-08": [
     "IDL-014",
     "IDL-044",
     "IDL-055"
    ]
   }
  },
  {
   "batch": "agent-waveA-exempt-2026-07-04",
   "date": "2026-07-04",
   "reviewer": "Wave A design fan-out (independent agent per row designed these as EXEMPT) + completeness-critic + author verification vs the pinned spec",
   "spec_reverified": true,
   "notes": "Three agent-bound identity MUSTs are IRREDUCIBLE in the in-process request/response reverse harness and documented in agent_exemptions.json (NOT a pass — a non-testability claim, mirroring the merchant exemptions discipline). IDL-006 (L119-120 'select the strongest token-endpoint auth method compatible with the platform's deployment model') — our reference is a PUBLIC client whose only valid method is 'none' (IDL-004), so 'select strongest' needs a confidential-capable client and a downgrade mutation can't be isolated from IDL-002/005. IDL-039 (L358-359 'ignore unrecognized fields in a per-scope policy object') — the agent never consumes per-scope policy objects so it satisfies this vacuously; the only violation (abort on unknown key) is a generic failure-to-proceed already caught by flow checks; no diagonal kill (distinct from IDL-057 = config-LEVEL unknown fields, which IS checked). IDL-056 (L646-649 'when config.providers absent (v1), fall back to direct OAuth 2.0 via RFC 8414') — the antecedent never arises in v1 so the consequent is the unconditional base flow already covered by IDL-010/031/062; no isolable diagonal. Each was independently DESIGNED as EXEMPT by a separate design agent + vetted by the batch completeness-critic + author-verified against the spec quotes. Denominator membership unchanged (these were GAPs, now EXEMPT).",
   "exempt_ids": {
    "2026-04-08": [
     "IDL-006",
     "IDL-039",
     "IDL-056"
    ]
   }
  }
 ]
}