{
  "_about": "Documented, reviewed exceptions for the differential gate (differential.py). Each entry silences ONE (target, check) deviation because the independent target is KNOWN to legitimately deviate on that point — e.g. a real, reported bug in that target, or a capability the target implements to an older pinned version. An entry needs the target name, the check id, and a substantive reason (with a link/citation where possible). This exists so a genuine target bug can never silently mask one of OUR check bugs: every deviation is either resolved (fix the check or the target) or explained here. An empty allow list is the healthy default — it means every check agrees with every independent conformant target we test.",
  "allow": [
    {
      "target": "flower-shop-official-sample",
      "check": "checkout.idempotency_conflict",
      "reason": "Reported and fixed upstream as Universal-Commerce-Protocol/samples#156. The target returns HTTP 500 (AttributeError on 'currency') for any checkout create that omits currency, and checkout.json annotates currency with ucp_request: omit, so the conformant body this check sends never reaches the idempotency path at all. The check itself is sound: given a body that does include currency the target returns 409 with an IDEMPOTENCY_CONFLICT envelope, exactly what CHK-048 requires. Remove this entry once #156 merges and the samples pin advances past it; the probe-hygiene gate fails on a known-defect entry that no longer reproduces, so a stale exception cannot be forgotten here.",
      "upstream": "https://github.com/Universal-Commerce-Protocol/samples/pull/156",
      "filed": "2026-07-31"
    }
  ]
}
